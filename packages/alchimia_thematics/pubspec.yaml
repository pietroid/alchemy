name: alchimia_thematics
description: Design tokens, colors, and theme configuration for Alchimia.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0
